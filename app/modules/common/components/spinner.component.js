import Controller from 'common/controllers/spinner.controller'
import template from 'views/common/spinner.template.html'

const component = {
    controller: Controller,
    template: template,
    controllerAs: 'vm'
}

export default component;